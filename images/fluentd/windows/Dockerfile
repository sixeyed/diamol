ARG FLUENTD_VERSION
ARG FLUENTD_RELEASE
ARG WINDOWS_VERSION
ARG DOCKER_BUILD_CPU
FROM fluent/fluentd:${FLUENTD_VERSION}-windows-${WINDOWS_VERSION}-${FLUENTD_RELEASE}

COPY fluent.conf /fluent/conf/fluent.conf
ENV FLUENTD_CONF="fluent.conf"