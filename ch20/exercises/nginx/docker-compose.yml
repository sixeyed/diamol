services:
  nginx:
    image: diamol/nginx:2e
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "./sites-enabled:/etc/nginx/sites-enabled"
      - "./certs:/etc/nginx/certs"
    networks:
      - ch20

networks:
  ch20:
    name: ch20    
    external: true
